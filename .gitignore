# ============================================================
# GitHub 上传忽略清单
#
# 注意：这份文件和 .vercelignore 是两套独立规则，管的是两件不同的事
#   .gitignore    → 谁进 GitHub 仓库
#   .vercelignore → 谁上 Vercel 生产环境
# 排除范围刻意不一样。典型例子：assets/aail-demo.mp4 要上 Vercel，但不进 GitHub。
# 以后新增敏感文件，两个文件都要记得改。
# ============================================================

# Vercel 本地关联与环境变量
# .vercel/project.json 是本机与 Vercel 项目的绑定，不该进仓库
# .env* 挡住 .env.local（当前含 vercel link 生成的 VERCEL_OIDC_TOKEN，
# 以后加的 DEEPSEEK_API_KEY 也一样安全）
.vercel
.env*

# 系统垃圾文件
.DS_Store
**/.DS_Store

# ------------------------------------------------------------
# 隐私与公司内部资料：绝不进仓库
#
# data/ 里有两类东西：
#   resume_detailed.pdf、实习主要事件.docx —— 简历原件与个人记录，含手机号
#   停车场收费.md、掘金高德.md、ai电话+营业时间.md、停车场图片数据获取.md
#     —— 百度实习期间的内部数据与策略记录
#
# 即使仓库是 Private 也不放：把公司内部资料存到个人云账号通常不符合保密要求，
# 而且仓库随时可能转 public —— 一旦转了，git 历史里的东西全部公开，
# 而 git 历史清不掉（要 filter-repo 重写整个历史）。
# 需要备份这些文件请用本地硬盘或公司允许的存储。
data/
# ------------------------------------------------------------

# ------------------------------------------------------------
# 视频素材：不进仓库
#
# assets/zhitan-demo.mp4 有 160 MB，超过 GitHub 单文件 100 MB 硬上限，
# 会让整个 push 被拒绝（不是跳过这个文件，是整次推送失败）。
# 职探demo.mov 71 MB、demo.mp4 58 MB 也会触发大文件警告。
#
# 部署走 vercel CLI 直传，视频由 .vercelignore 单独管，不经过 GitHub。
# 四个视频全部排除，它们仍留在本机、线上不受影响。
#
# 例外提醒：将来若改成 git push 自动部署，assets/aail-demo.mp4（42 MB）
# 必须进仓库，否则线上 AAIL demo 会 404。届时这条规则要改。
*.mp4
*.mov
# ------------------------------------------------------------

# Comate 的规格文档与工作记录
.comate/
